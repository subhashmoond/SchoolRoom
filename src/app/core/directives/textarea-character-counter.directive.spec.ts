import { TextareaCharacterCounterDirective } from './textarea-character-counter.directive';

describe('TextareaCharacterCounterDirective', () => {
  it('should create an instance', () => {
    const directive = new TextareaCharacterCounterDirective();
    expect(directive).toBeTruthy();
  });
});
