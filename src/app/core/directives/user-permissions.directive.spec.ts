import { UserPermissionsDirective } from './user-permissions.directive';

describe('UserPermissionsDirective', () => {
  it('should create an instance', () => {
    // const directive = new UserPermissionsDirective(new,'');
    expect('directive').toBeTruthy();
  });
});
