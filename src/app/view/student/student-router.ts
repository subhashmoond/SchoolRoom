import { Routes } from '@angular/router';
import { LoginMainStudentComponent } from './login/login-main/login-main.component';

export const routes: Routes = [
    { path: '', component: LoginMainStudentComponent },
];

